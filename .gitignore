# Build output
target/
*.class
dependency-reduced-pom.xml

# Local data / logs produced during runs
data/
**/data/
**/app.log
**/app.err.log
results/grade-*.log
*.stackdump

# Stray tool output that can land at the repo root
/META-INF/
/io/
/org/

# Per-trial contestant workspaces are run evidence, not source.
# They are regenerated by copying a golden scaffold (see RUNNING.md) and are
# intentionally NOT published (avoids leaking solutions to agents under test).
/runs/*
!/runs/README.md

# OS / IDE
.DS_Store
.idea/
*.iml
